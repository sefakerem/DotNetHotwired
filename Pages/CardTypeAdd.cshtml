@model Hotwired.Models.CardType
<turbo-stream action="replace" target="type-0">
    <template>
        <turbo-frame id="type-0"></turbo-frame>
    </template>

</turbo-stream>

<turbo-stream action="append" target="card-types">
    <template>
        <partial name="CardTypeView" model="@Model"></partial>
    </template>

</turbo-stream>